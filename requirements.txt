# Sacred Texts RAG — Python Dependencies
# Install with: pip install -r requirements.txt

# UI
streamlit>=1.35.0

# Vector database
chromadb>=0.5.0

# Embeddings (runs locally, no API key needed)
sentence-transformers>=3.0.0

# LLM (Groq — free tier)
groq>=0.9.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.26.0
